
<section>
  <h1>NSFW Gallery</h1>
  <p>Content flagged as not-safe-for-work. Viewer discretion advised.</p>
  <form method="get">
    <input name="q" placeholder="Search art..." value="<%= q || '' %>" />
    <button>Search</button>
  </form>
  <div class="grid">
    <% arts.forEach(a => { %>
      <figure>
        <img src="<%= a.image_path %>" alt="<%= a.title %>" />
        <figcaption><%= a.title %> — <em><%= a.artist_name %></em></figcaption>
      </figure>
    <% }) %>
  </div>
  <%- include('../partials/pagination', { total, page, limit, q }) %>
</section>
